import Table from './Table'

import Head, { TableHeadProps } from './Head'
import HeadRow, { TableHeadRowProps } from './HeadRow'
import Header, { TableHeaderProps } from './Header'

import Body, { TableBodyProps } from './Body'
import Row, { TableRowProps } from './Row'
import Cell, { TableCellProps } from './Cell'

export { Table, Head, HeadRow, Header, Body, Row, Cell }

export type {
  TableHeadProps,
  TableHeadRowProps,
  TableHeaderProps,
  TableBodyProps,
  TableRowProps,
  TableCellProps,
}
